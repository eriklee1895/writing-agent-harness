# Brief 2: TanStack Ecosystem Map

A clean flat-tech infographic showing the TanStack ecosystem as a layered modular stack. English labels only.

Layout: 5 horizontal swimlanes on a soft off-white background.

Lane 1 (top, framework layer): one large rounded module containing two sub-modules labeled "Start" and "Router" - the meta-frameworks.

Lane 2 (data and state management): four modules in a row - "Query" (with a small server/cloud icon), "DB" (database cylinder icon), "Store" (state container icon), "AI" (neural network icon).

Lane 3 (UI and UX): three modules - "Table" (grid icon), "Form" (form lines icon), "Hotkeys" (keyboard icon).

Lane 4 (performance): two modules - "Virtual" (stacked layers icon), "Pacer" (clock/debounce icon).

Lane 5 (tooling, bottom): four modules - "Devtools" (wrench icon), "Config" (gear icon), "CLI" (terminal icon), "Intent" (target icon).

A thin dashed vertical bracket on the right side of all lanes.

Color palette: muted teal/mint green (#32E6E2) as primary accent, soft warm grays, white background, very subtle drop shadows. All modules have rounded corners and consistent stroke weight.

Style: flat-tech-infographic, clean, professional, document-friendly, ample whitespace.
