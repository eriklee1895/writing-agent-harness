# 文字海报 / Banner / 封面（Text Poster）

> Pro-exclusive 风格 preset。Seedream 5.0 Pro 的中文/英文文字渲染是 headline 能力，这个 preset 覆盖科技博客封面、杂志封面、宽幅 banner/封面（16:9 —— blog hero、公众号头图、YouTube 缩略图、PPT 首页、视频封面等）、信息图、电商 banner、徽章 logo 六大高频场景。
> Lite 不推荐使用此 preset（文字质量明显下降）。

## 结构 Guardrails（所有文字海报通用）

1. **文字内容必须逐字准确**——prompt 里用引号/书名号把文字内容写全，不要写"一个关于 XX 的标题"。
2. **先定版式再写字**——先讲背景色/材质/版式分区，再写文字内容，最后说字体字重字号颜色位置对齐。
3. **字数控制**：主标题 ≤12 字 / 英文 ≤4 词最稳；副标题 ≤20 字/英文 ≤8 词；信息图标签每个 ≤4 字。
4. **字体族用通用描述**——不要指定具体字体名（"思源宋体""Helvetica Neue" Pro 不认识），用族名：
   - 中文字：`粗黑无衬线` / `细无衬线` / `宋体` / `楷体` / `手写毛笔字` / `圆润卡通体` / `等宽代码字体`
   - 英文字：`bold sans-serif` / `thin sans-serif` / `serif (Bodoni-style)` / `script/italic` / `monospace` / `geometric sans`
5. **字号用比例描述**——`主标题占画面高度 1/5`、`副标题字号是主标题的 1/3`、不要写"字号 72pt"（模型不理解 pt）。
6. **位置用相对描述**——`顶部居中`、`左上 1/3 处`、`底部留白 20%`。
7. **显式说对齐**——`居中对齐` / `左对齐留 10% 边距` / `右对齐`。
8. **2K 起步**——1K 文字边缘糊，默认 `--size 2K`；16:9 宽幅封面用 `--wide`（1792×1024 Pro-only，公众号头图等场景通用）。
9. **一次只放一个主标题**——多个标题层级（主+副+tag）可以，但不要两个同等重要的大标题抢视觉。

## Inspiration Defaults（可被具体 brief 覆盖）

- 默认字体：粗黑无衬线（中文）+ geometric sans-serif（英文）——现代专业感最强，模型最稳
- 默认字色：白或深黑（对比强，错字肉眼容易发现）
- 默认背景：纯色 / 柔和渐变 / 纸张质感（ busy 的背景会干扰文字渲染）
- 默认装饰：1–3 个几何小图形点缀（不挡文字）
- 默认留白：四周 ≥8% 边距，避免裁切

---

## Recipes

### Recipe 1：科技博客封面（中文，竖版 3:4）

```bash
uv run scripts/seedream_image_gen.py generate \
  --size 2K \
  --prompt "极简科技博客封面，浅灰米色纸张质感背景，
居中黑色粗无衬线大标题写着「大模型时代的写作」，字号占画面高度 1/6，
下方一行较小灰色副标题「AI 与创作者的新共生关系」，
右下角一枚蓝色极简几何芯片线性图标（不挡文字），
顶部 10% 留白，底部 15% 留白，
整体杂志编辑设计感，锐利印刷质感，2K，3:4 竖版"
```

- 关键词：纸张质感 + 居中大标题 + 单图标点缀
- 适合：技术长文封面、专栏/博客头图

### Recipe 2：宽幅 16:9 封面 / Banner（--wide，Pro 专属）

```bash
uv run scripts/seedream_image_gen.py generate --wide \
  --prompt "16:9 宽幅技术博客封面 banner，左 60% 区域为纯白/浅灰渐变背景，
左侧粗黑色无衬线主标题「Claude 5 深度解析」左对齐、字号占高度 1/4，
其下一行较小灰色副标题「Agentic Coding 的下一个十年」，
右侧 40% 区域是深蓝色抽象粒子/光束装饰图形，
左侧与右侧之间有 5% 留白呼吸区，
整体干净锐利有编辑感，2K"
```

- 关键：用 `--wide` 自动 1792×1024（Pro only，推荐）；`--wechat-header` 是同尺寸别名，兼容老调用。
- 版式：文字集中在左/中，避免右侧——多数平台预览（公众号列表裁切右侧、YouTube/社交分享缩略图右侧易被遮挡/裁切）都会裁掉右边缘，关键信息靠左/居中更安全。
- 把「Claude 5 深度解析」/「Agentic Coding 的下一个十年」替换成你自己的主/副标题即可，保持"主标题 ≤12 字、副标题 ≤20 字一行"的字数上限。

### Recipe 3：中英混排信息图（横版 16:9）

```bash
uv run scripts/seedream_image_gen.py generate --size 1792x1024 \
  --prompt "技术信息图横版海报，白底，顶部居中大号深蓝粗无衬线中文标题「AI 编程三阶段」，
其下一行较小英文副标题「Three Eras of AI Coding」，中英字体字号视觉平衡，
画面中部水平排列三个蓝色圆角矩形卡片，每个卡片中央白色短标签文字，
从左到右分别是「补全」、「对话」、「自主」，字号适中、清晰锐利，
卡片下方有细小英文小字「Autocomplete」/「Chat」/「Agentic」，
卡片之间用细灰色箭头连接，底部浅灰小字来源标注，
扁平化矢量风格，2K"
```

- 卡片内标签 ≤2–4 字最稳
- 注意底部小字描述可能略糊，小字信息不要太密

### Recipe 4：电商 Banner（16:9，大字促销）

```bash
uv run scripts/seedream_image_gen.py generate --size 1792x1024 \
  --prompt "电商 618 大促 banner，暖红色渐变背景带金色粒子光效，
居中金色粗宋体大字写着「年中大促」，字号占画面高度 1/3、醒目突出，
大字下方一行白色小号副标题「全场 5 折起」，
左右两侧点缀抽象金色丝带/礼盒/金币装饰元素（不挡文字），
节日氛围浓厚、视觉冲击力强、印刷质感锐利，2K，16:9"
```

- 促销类用金色/红色/宋体比无衬线更有节庆感
- 可以加 `--no-negative-prompt` 放开"多余肢体"类的负面限制（电商图不一定有人）

### Recipe 5：杂志封面（人物 + 大标题）

```bash
uv run scripts/seedream_image_gen.py generate \
  --size 2K \
  --prompt "时尚杂志封面，右侧 2/3 是一位短发东亚女性时尚编辑的特写肖像，
穿黑色高领毛衣、戴金色几何耳饰，自信看向镜头，工作室硬光，中画幅景深，
左侧 1/3 留白区域是大号白色衬线字体竖排英文标题「THE NEW INTELLIGENCE」，
顶部有小号白色无衬线刊名「VERGE」，
底部 10% 有小号白色英文文章标题列表占位文字，
Vogue 封面美学，黑白金三色配色，2K，3:4 竖版"
```

- 人物 + 文字混合场景：把人物位置和文字位置**分区明确**（左/右、上/下），避免文字落在人物脸上
- 衬线英文 (Bodoni-style) 是时尚杂志的经典选择

### Recipe 6：圆形徽章 Logo（方图）

```bash
uv run scripts/seedream_image_gen.py generate --square \
  --prompt "圆形徽章 logo，深藏青色圆形底、金色外圈双细线，
白色粗衬线英文字母沿徽章顶部弧形排列「SEEDREAM PRO」，
徽章正中央是红色篆书风格「豆包」两个中文字，
徽章底部是小号金色字「2026」，
徽章两侧伸出金色月桂枝装饰，
纯白背景，矢量扁平化风格，边缘锐利干净，1024x1024"
```

- `--square` → 1024×1024（Pro）
- 关键 phrase："沿徽章顶部弧形排列"——模型对弧形字识别很准
- 篆书/印章风中文 Pro 表现不错

---

## 文字翻车排查清单

如果出图文字不对，按顺序排查：

1. **文字是否在 prompt 里用引号完整写出？**（没引号 = 模型自己填字）
2. **是否用了 2K？**（1K 文字糊；Lite 错字多）
3. **是否显式指定了字体族 + 字重？**（没说 = 随机字体）
4. **是否说了位置和对齐？**（没说 = 文字到处飘）
5. **单条文字长度是否 ≤12 字？**（太长会错字/糊）
6. **背景是否干净？**（复杂纹理背景下文字边缘会脏）
7. **再跑一次**——同 prompt 第二次常能修好偶发错字

仍错的话：加 "字迹清晰锐利、每个字都准确无误、印刷级清晰度" 重试；还是错就降复杂度（缩短文字、加大字号、纯色背景）。
