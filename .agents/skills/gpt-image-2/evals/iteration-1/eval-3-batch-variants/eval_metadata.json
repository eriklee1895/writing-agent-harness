{
  "eval_id": 3,
  "eval_name": "batch-variants",
  "prompt": "Generate three variants for a product card series from /tmp/eval-fixtures/batch-prompts.jsonl: 1) a vintage travel poster with EXACT verbatim text 'KYOTO SPRING' (letter-by-letter K-Y-O-T-O S-P-R-I-N-G) in square format at high quality, 2) a studio shot of a fountain pen in portrait at medium quality, 3) a flat 3D low-poly arctic fox in square. Output to output/gpt-image-2/batch/.",
  "files": ["/tmp/eval-fixtures/batch-prompts.jsonl"],
  "assertions": [
    {
      "name": "three output images are created",
      "description": "output/gpt-image-2/batch/ contains three .png files (one per job) plus metadata .json"
    },
    {
      "name": "sizes match per-job overrides",
      "description": "The metadata .json files record square / portrait / square as the size"
    },
    {
      "name": "qualities match per-job overrides",
      "description": "The metadata .json files record high / medium / auto (or unset) qualities"
    },
    {
      "name": "metadata is consistent across the batch",
      "description": "All three .json files share the same model and base_url"
    }
  ]
}
